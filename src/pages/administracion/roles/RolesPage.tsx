import { PageStub } from "@/components/PageStub";

export function RolesPage() {
  return (
    <PageStub
      title="Roles y permisos"
      description="Edición completa de qué permisos tiene asignado cada rol."
      block="Bloque 11 (requiere endpoints de backend definidos en el prompt de Fase 2.5)"
    />
  );
}
